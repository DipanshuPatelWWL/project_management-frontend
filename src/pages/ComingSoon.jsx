const ComingSoon = ({ label }) => {
    return (
        <div style={{ padding: "24px" }}>
            <h1>{label}</h1>
            <p style={{ color: "#888" }}>This page hasn't been built yet.</p>
        </div>
    );
};

export default ComingSoon;